"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = translateText;
const randomString_1 = __importDefault(require("../randomString"));
async function translateText(text, from, to, endpoint, key, location) {
    const filteredEndpoint = endpoint.replace(/\/$/, "");
    const toDestructured = to.map((lang) => `to=${lang}`).join("&");
    const url = `${filteredEndpoint}/translate?api-version=3.0&from=${from}&${toDestructured}`;
    const headers = {
        "Ocp-Apim-Subscription-Key": key,
        "Ocp-Apim-Subscription-Region": location,
        "Content-type": "application/json",
        "X-ClientTraceId": (0, randomString_1.default)(),
    };
    const response = await fetch(url, {
        method: "POST",
        headers,
        body: JSON.stringify([{ text }]),
    });
    return await response.json();
}
